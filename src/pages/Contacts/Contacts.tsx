import ContactsList from "../../Component/Contacts/ContactsList";
import "./Contacts.scss";

type Props = {};

const Contacts = (props: Props) => {
  return (
    <main>
      <ContactsList />
    </main>
  );
};

export default Contacts;
